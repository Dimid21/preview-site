/* =========================================================
   Modern UI theme – Klinik Tirta Husada
   Palette: Morning Cocktail (#BFC382, #D8D8D9, #2E765E, #8DA33B)
   ========================================================= */

:root {
    /* Glass system — Bright Premium Version */
    --glass-radius-lg: 28px;
    --glass-radius-md: 20px;

    /* Glass background lebih terang & lebih menyala */
    --glass-bg-strong: rgba(255, 255, 255, 0.58); /* dari 0.35 → 0.58 */
    --glass-bg-soft: rgba(255, 255, 255, 0.28); /* dari 0.15 → 0.28 */

    /* Border lebih putih (tampilan kaca lebih nyata) */
    --glass-border: rgba(255, 255, 255, 0.75); /* dari 0.40 → 0.75 */

    /* Blur lebih tinggi = efek frosted lebih lembut & premium */
    --glass-blur: 26px; /* dari 18px → 26px */
    --glass-sat: 190%; /* sedikit lebih vivid */

    /* Shadow lebih besar & lebih soft untuk floating feel */
    --glass-shadow: 0 30px 55px rgba(2, 16, 32, 0.12);
    --glass-shadow-hover: 0 40px 80px rgba(0, 0, 0, 0.18);

    /* Brand */
    --brand-600: #2e765e; /* hijau gelap */
    --brand-500: #8da33b; /* hijau muda */
    --brand-400: #bfc382;
    --brand-300: #d8d8d9;
    --brand-200: #d8d8d9;
    --brand-100: #d8d8d9;

    --brand: var(--brand-600);
    --brand-green: var(--brand-600);
    --brand-green-dark: #195243;
    --btn: var(--brand-500);

    /* Ink & surfaces */
    --ink-900: #0f172a;
    --ink-700: #334155;
    --ink-500: #64748b;

    --bg: #d8d8d9;
    --card: #ffffff;
    --ring: #e5e7eb;

    --rad: 16px;
    --shadow: 0 12px 30px rgba(2, 6, 23, 0.18);
}

/* Base typography & layout
   -------------------------------------------------------- */

html,
body {
    scroll-behavior: smooth;
}

body {
    background: var(--bg);
    color: var(--ink-700);
    font:
        16px/1.6 system-ui,
        -apple-system,
        Segoe UI,
        Roboto,
        Inter,
        Helvetica,
        Arial,
        sans-serif;
}

h1,
h2,
h3 {
    color: var(--ink-900);
    letter-spacing: -0.01em;
}

h1 {
    font-size: clamp(28px, 4vw, 44px);
    font-weight: 800;
}

h2 {
    font-size: clamp(22px, 3vw, 32px);
    font-weight: 800;
}

h3 {
    font-size: clamp(18px, 2.4vw, 24px);
    font-weight: 700;
}

.muted {
    color: var(--ink-500);
}

.container {
    max-width: 1200px;
    margin-inline: auto;
    padding-inline: 16px;
}

/* Section spacing: satu sumber saja */
.section {
    padding: 48px 24px;
}

@media (min-width: 768px) {
    .section {
        padding: 72px 24px;
    }
}

/* Generic card / glass
   -------------------------------------------------------- */

.card {
    background: var(--card);
    border-radius: var(--rad);
    box-shadow: var(--shadow);
    border: 1px solid var(--ring);
}

.glass {
    backdrop-filter: saturate(140%) blur(8px);
    background: rgba(255, 255, 255, 0.8);
    border: 1px solid rgba(255, 255, 255, 0.75);
}

/* Buttons
   -------------------------------------------------------- */

.btn {
    display: inline-flex;
    gap: 0.5rem;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    border-radius: 14px;
    padding: 0.7rem 1.1rem;
    border: 1.5px solid transparent;
    transition: 0.2s ease;
    cursor: pointer;
}

.btn:focus {
    outline: none;
    box-shadow: 0 0 0 4px rgba(57, 147, 205, 0.15);
}

.btn-primary {
    background: linear-gradient(
        135deg,
        rgba(46, 118, 94, 0.95),
        rgba(141, 163, 59, 0.95)
    );
    color: #fff;
    box-shadow: 0 6px 20px rgba(46, 118, 94, 0.25);
    border: 1px solid rgba(255, 255, 255, 0.35);
    backdrop-filter: blur(6px);
    transition: 0.25s;
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 28px rgba(46, 118, 94, 0.34);
}

.btn-ghost {
    background: #fff;
    color: var(--brand);
    border-color: var(--brand-300);
}

.btn-ghost:hover {
    background: var(--brand-100);
}

/* Outline button (lokasi section) */
.btn-outline {
    display: inline-flex;
    align-items: center;
    padding: 10px 18px;
    border-radius: 14px;
    font-weight: 600;
    color: var(--brand-600);
    border: 1px solid var(--brand-600);
    backdrop-filter: blur(4px);
    background: rgba(255, 255, 255, 0.45);
    transition: 0.25s;
}

.btn-outline:hover {
    background: rgba(255, 255, 255, 0.7);
    border-color: var(--brand-500);
    color: var(--brand-500);
    transform: translateY(-2px);
}

/* Chips */
.chip {
    display: inline-block;
    font-weight: 700;
    font-size: 0.75rem;
    padding: 0.35rem 0.55rem;
    border-radius: 10px;
    border: 1px solid rgba(57, 147, 205, 0.3);
    color: var(--brand);
    background: rgba(232, 244, 252, 0.8);
}

/* Top bar & navbar
   -------------------------------------------------------- */

.topbar {
    background: var(--brand);
    color: #fff;
}

.navbar {
    position: sticky;
    top: 0;
    z-index: 40;
    background: #fff;
    border-bottom: 1px solid var(--ring);
    backdrop-filter: saturate(120%) blur(6px);
}

.nav-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 14px 0;
}

.nav-links a {
    color: var(--ink-700);
    font-weight: 600;
    padding: 8px 10px;
    border-radius: 10px;
}

.nav-links a:hover {
    background: var(--brand-100);
    color: var(--brand);
    text-decoration: none;
}

/* Hero section
   -------------------------------------------------------- */

.hero-wrap {
    position: relative;
    margin: 0;
}

/* Background image hero (parallax sederhana) */
.hero-media {
    position: relative;
    width: 100%;
    height: 420px;
    overflow: hidden;

    background-image: url("assets/hero-full.jpg");
    background-size: cover;
    background-position: center center;
    background-attachment: fixed;
}

@media (min-width: 1024px) {
    .hero-media {
        height: 520px;
    }
}

.hero-media img {
    display: none;
}

/* Overlay isi hero */
.hero-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
}

/* Konten kiri hero */
.hero-left {
    max-width: 1200px;
    margin: auto;
    padding: 0 16px;
    width: 100%;
    margin-left: 150px;
    margin-right: auto;
}

/* Glass hero box */
.hero-box {
    max-width: 680px;
    padding: 24px;
    border-radius: 28px;

    /* super clear glass */
    background: linear-gradient(
        135deg,
        rgba(255, 255, 255, 0.26),
        rgba(255, 255, 255, 0.12)
    );
    border: 1px solid rgba(255, 255, 255, 0.4);

    backdrop-filter: blur(18px) saturate(170%);
    -webkit-backdrop-filter: blur(18px) saturate(170%);

    box-shadow:
        0 3px 8px rgba(255, 255, 255, 0.45) inset,
        0 14px 32px rgba(15, 23, 42, 0.14);
}

@media (min-width: 768px) {
    .hero-box {
        padding: 28px;
    }
}

/* Ilustrasi kanan hero */
.hero-slider {
    position: absolute;
    right: 3%;
    top: 50%;
    transform: translateY(-50%);
    border-radius: 0;
    overflow: visible;
    border: none;
    box-shadow: none;
    background: transparent;
}

.hero-slide-img {
    width: 720px;
    height: auto;
    object-fit: contain;
    display: block;
    background: transparent;
}

/* Grid helper */
.grid-3 {
    display: grid;
    gap: 20px;
}

@media (min-width: 1024px) {
    .grid-3 {
        grid-template-columns: repeat(3, 1fr);
    }
}

/* Links, hr, form, table, partners
   -------------------------------------------------------- */

a {
    text-decoration: none;
    color: var(--brand);
}

a:hover {
    text-decoration: underline;
}

hr {
    border: none;
    border-top: 1px solid var(--ring);
    margin: 24px 0;
}

.field {
    border: 1px solid var(--ring);
    background: #fff;
    border-radius: 12px;
    padding: 0.65rem 0.8rem;
    width: 100%;
}

.field:focus {
    outline: none;
    border-color: var(--brand-300);
    box-shadow: 0 0 0 4px rgba(57, 147, 205, 0.12);
}

.table {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    overflow: hidden;
    border-radius: 12px;
    border: 1px solid var(--ring);
}

.table th,
.table td {
    padding: 12px 14px;
    border-bottom: 1px solid var(--ring);
}

.table thead th {
    background: var(--brand-100);
    color: var(--ink-900);
    text-align: left;
}

.partners {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    align-items: center;
    opacity: 0.9;
}

/* Animasi halus */
.fade-in {
    opacity: 0;
    transform: translateY(6px);
    animation: fade 0.5s ease forwards;
}

@keyframes fade {
    to {
        opacity: 1;
        transform: none;
    }
}

/* Scrollbar */
::-webkit-scrollbar {
    height: 10px;
    width: 10px;
}

::-webkit-scrollbar-thumb {
    background: var(--brand-300);
    border-radius: 999px;
}

::-webkit-scrollbar-thumb:hover {
    background: var(--brand-400);
}

/* Tailwind teal overrides (satu blok saja)
   -------------------------------------------------------- */

.bg-teal-700,
.bg-teal-600,
.bg-teal-500 {
    background: #2e765e !important;
}

.bg-teal-100 {
    background: #d8d8d9 !important;
}

.text-teal-700 {
    color: #2e765e !important;
}

.border-teal-700 {
    border-color: #2e765e !important;
}

/* Lokasi Kami – Morning Cocktail
   -------------------------------------------------------- */

.section-lokasi {
    background: #d8d8d9;
}

/* Glass card global (dipakai lokasi & artikel) */
.glass-card {
    background: linear-gradient(
        135deg,
        var(--glass-bg-strong),
        var(--glass-bg-soft)
    );
    border-radius: var(--glass-radius-lg);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    box-shadow: var(--glass-shadow);
    transition: 0.25s ease;
}

.glass-card:hover {
    box-shadow: var(--glass-shadow-hover);
    transform: translateY(-4px);
}

/* Versi lebih kompak */
.glass-card--compact {
    border-radius: var(--glass-radius-md);
    backdrop-filter: blur(14px) saturate(160%);
    -webkit-backdrop-filter: blur(14px) saturate(160%);
    box-shadow: 0 18px 40px rgba(15, 23, 42, 0.12);
}

/* Panel glass besar (kalau dipakai) */
.glass-panel {
    background: linear-gradient(
        145deg,
        rgba(255, 255, 255, 0.3),
        rgba(255, 255, 255, 0.1)
    );
    border-radius: 32px;
    border: 1px solid rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(22px) saturate(180%);
    -webkit-backdrop-filter: blur(22px) saturate(180%);
    box-shadow: 0 30px 60px rgba(15, 23, 42, 0.18);
    padding: 2rem;
}

/* Glass button khusus */
.glass-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.6rem 1.4rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.6);
    background: linear-gradient(
        135deg,
        rgba(255, 255, 255, 0.45),
        rgba(255, 255, 255, 0.2)
    );
    color: var(--brand-green-dark);
    font-weight: 600;
    backdrop-filter: blur(18px) saturate(170%);
    -webkit-backdrop-filter: blur(18px) saturate(170%);
    box-shadow: 0 10px 25px rgba(15, 23, 42, 0.18);
    cursor: pointer;
    text-decoration: none;
    transition: 0.2s ease;
}

.glass-button:hover {
    background: linear-gradient(
        135deg,
        rgba(255, 255, 255, 0.7),
        rgba(255, 255, 255, 0.3)
    );
    transform: translateY(-1px);
    box-shadow: 0 16px 35px rgba(15, 23, 42, 0.25);
}

/* Glass badge kecil (lokasi) */
/* Glass badge untuk Klinik Pusat / Cabang – tinted green */
.glass-badge {
    background: rgba(
        46,
        118,
        94,
        0.12
    ); /* hijau sangat muda, kontras di glass putih */
    border: 1px solid rgba(46, 118, 94, 0.35); /* outline hijau lembut */
    backdrop-filter: blur(10px); /* masih ada efek glass tipis */

    padding: 4px 12px;
    border-radius: 999px;

    color: #195243; /* hijau gelap, mudah dibaca */
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
}

/* Optional glass nav */
.glass-nav {
    background: linear-gradient(
        90deg,
        rgba(15, 23, 42, 0.55),
        rgba(15, 23, 42, 0.4)
    );
    backdrop-filter: blur(22px) saturate(180%);
    -webkit-backdrop-filter: blur(22px) saturate(180%);
    border-bottom: 1px solid rgba(255, 255, 255, 0.25);
}

/* =========================================
   Layanan – Glass Card tanpa hover
========================================= */

.service-card {
    /* copy dari .glass-card */
    background: linear-gradient(
        135deg,
        var(--glass-bg-strong),
        var(--glass-bg-soft)
    );
    border-radius: var(--glass-radius-lg);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    box-shadow: var(--glass-shadow);

    /* boleh tetap ada transition kecil untuk isi (opsional) */
    transition: none;
}

/* Matikan efek hover (tidak ada perubahan bayangan / posisi) */
.service-card:hover {
    box-shadow: var(--glass-shadow);
    transform: none;
}

/* =============================================
   Jadwal Dokter — Glass Card Besar
============================================= */
.schedule-card {
    background: linear-gradient(
        135deg,
        var(--glass-bg-strong),
        var(--glass-bg-soft)
    );
    border-radius: var(--glass-radius-lg);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    box-shadow: var(--glass-shadow);
    padding: 40px;
}

/* Tinggi kolom konsisten */
.schedule-column,
.schedule-table {
    min-height: 420px; /* kamu bisa naikkan 300 / 320 sesuai selera */
}

/* Panel tabel */
.schedule-table {
    background: rgba(255, 255, 255, 0.38);
    border: 1px solid rgba(255, 255, 255, 0.6);
    border-radius: var(--glass-radius-md);
    backdrop-filter: blur(18px) saturate(160%);
    -webkit-backdrop-filter: blur(18px) saturate(160%);
    box-shadow: 0 18px 35px rgba(15, 23, 42, 0.08);

    padding: 0; /* hilangkan padding, biar iframe full */
    overflow: hidden; /* supaya radius card kepotong rapi */
    min-height: 420px; /* tinggi yang sudah kamu pilih */
}

.schedule-table iframe {
    width: 100%;
    height: 100%;
    border: none;
}

/* Tabs Lokasi Jadwal */
.schedule-tabs {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
}

.schedule-tab {
    padding: 0.35rem 0.9rem;
    border-radius: 999px;
    border: 1px solid rgba(46, 118, 94, 0.35);
    background: #ffffff;
    color: #195243;
    font-size: 0.9rem;
    font-weight: 600;
    cursor: pointer;
    transition: 0.15s ease;
}

.schedule-tab:hover {
    background: #f1faf8;
}

.schedule-tab.is-active {
    background: #2e765e;
    color: #ffffff;
    border-color: #2e765e;
}

/* Frame PDF di dalam glass */
.schedule-frame {
    width: 100%;
    height: calc(420px - 48px); /* 420 tinggi total, 48px untuk area tabs */
    border-radius: 16px;
    overflow: hidden;
}

.schedule-frame iframe {
    width: 100%;
    height: 100%;
    border: none;
}

/* =========================================
   Kontak Kami – Glass Card
========================================= */

/* Card besar (bungkus teks + form) */
.contact-card {
    background: linear-gradient(
        135deg,
        var(--glass-bg-strong),
        var(--glass-bg-soft)
    );
    border-radius: var(--glass-radius-lg);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    box-shadow: var(--glass-shadow);
    padding: 40px;
}

/* Card form di kanan */
.contact-form-card {
    background: rgba(255, 255, 255, 0.82);
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.85);
    box-shadow: 0 18px 36px rgba(15, 23, 42, 0.1);
    padding: 24px 24px 28px;
}

/* Biar form field tetap rapi di dalam glass */
.contact-form-card input,
.contact-form-card textarea {
    background-color: #ffffff;
    border-radius: 999px;
    border: 1px solid #e5e7eb;
}

.contact-form-card textarea {
    border-radius: 16px;
}

/* Tombol submit di dalam form */
.contact-form-card .btn-submit {
    width: 100%;
    border-radius: 999px;
}

/* Banner hijau di bawah kartu lokasi */
.section-lokasi .card .bg-teal-600,
.section-lokasi .card .bg-teal-700 {
    background: #2e765e !important;
    color: #ffffff !important;
}

/* Tombol Lokasi / WhatsApp */
.section-lokasi .btn-primary {
    background: #8da33b;
    border-color: #8da33b;
}

.section-lokasi .btn-ghost {
    color: #2e765e;
    border-color: #2e765e;
}

/* Tailwind bg-gray-50 → palet Morning Cocktail */
.bg-gray-50 {
    background-color: #d8d8d9 !important;
}

/* Artikel / Blog section
   -------------------------------------------------------- */

/* Section artikel (beranda & halaman artikel) pakai background sama dengan lokasi
   supaya layout konsisten */
.section-articles,
.section-articles--home {
    background: #d8d8d9;
}

.section-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 28px;
    margin: 0 auto 32px;
    max-width: 1500px;
}

.section-header h1,
.section-header h2 {
    font-size: clamp(24px, 3vw, 32px);
    font-weight: 700; /* sama dengan heading besar lain */
}

.section-subtitle {
    margin: 0;
    color: #475569;
    font-size: 1rem;
}

.section-header--with-link .section-link {
    margin-top: 4px;
}

.section-link {
    font-size: 0.9rem;
    font-weight: 600;
    color: #0f766e;
    text-decoration: none;
}

.section-link:hover {
    text-decoration: underline;
}

/* Grid artikel */
.article-grid {
    display: grid;
    max-width: 1500px;
    margin: 0 auto;
    gap: 32px;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
}

/* Beranda: 4 kolom di desktop */
@media (min-width: 1024px) {
    .article-grid--home {
        grid-template-columns: repeat(4, 1fr);
    }
}

/* Kartu artikel (base) */
.article-card {
    background: #ffffff;
    border-radius: 28px;
    padding: 32px;
    box-shadow: 0 25px 55px rgba(15, 23, 42, 0.1);
    display: flex;
    flex-direction: column;
    gap: 16px;
    min-height: 250px;
    transition: 0.25s ease;
}

.article-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 35px 75px rgba(0, 0, 0, 0.15);
}

/* Bila diberi .glass-card, pakai glass style global */
.article-card.glass-card {
    background: linear-gradient(
        135deg,
        var(--glass-bg-strong),
        var(--glass-bg-soft)
    );
    border-radius: var(--glass-radius-lg);
    border: 1px solid var(--glass-border);
    backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    -webkit-backdrop-filter: blur(var(--glass-blur)) saturate(var(--glass-sat));
    box-shadow: var(--glass-shadow);
}

.article-card.glass-card:hover {
    box-shadow: var(--glass-shadow-hover);
    transform: translateY(-4px);
}

.article-card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.9rem;
}

.article-date {
    color: #6b7280;
    font-size: 0.85rem;
}

.article-title {
    font-size: 1.25rem;
    font-weight: 700;
    margin: 0;
    line-height: 1.35;
}

.article-excerpt {
    font-size: 1rem;
    color: #4b5563;
    margin: 0;
    line-height: 1.55;
}

.article-link {
    margin-top: auto;
    font-weight: 600;
    font-size: 0.95rem;
    color: var(--brand-600);
}

/* Badge artikel – ukuran sama dengan glass-badge */
/* Badge artikel tanpa blur */
.article-tag {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 4px 12px;
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    border-radius: 999px;

    /* remove blur & glass */
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
    border: none !important;

    /* warna teks konsisten */
    color: #ffffff;
}

/* Promo */
.article-tag--promo {
    background: linear-gradient(
        135deg,
        rgba(15, 118, 110, 0.95),
        rgba(15, 118, 110, 0.8)
    );
}

/* Event */
.article-tag--event {
    background: linear-gradient(
        135deg,
        rgba(37, 99, 235, 0.98),
        rgba(37, 99, 235, 0.85)
    );
}

/* Artikel / Blog */
.article-tag--artikel,
.article-tag--blog {
    background: linear-gradient(
        135deg,
        rgba(22, 163, 74, 0.98),
        rgba(22, 163, 74, 0.85)
    );
}

/* Thumbnail untuk kartu artikel */
.article-card--with-thumb {
    /* turunkan isi kartu sedikit dari atas */
    padding-top: 16px;
}

.article-thumb {
    width: 100%;
    height: 160px;
    overflow: hidden;
    border-radius: 20px;

    /* beri sedikit ruang dari tepi atas kartu,
       dan tetap ada jarak ke konten di bawah */
    margin-top: 4px;
    margin-bottom: 20px;

    box-shadow: 0 12px 25px rgba(15, 23, 42, 0.18);
}

.article-thumb img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
}

/* Responsive tweaks */
@media (max-width: 767px) {
    .section-header {
        flex-direction: column;
        align-items: flex-start;
    }

    .article-card {
        border-radius: 18px;
    }
}

/* ==== Hero primary button fix (Lihat Lokasi) ==== */

.hero-box .btn-primary {
    /* buang gradient sebelumnya */
    background-image: none !important;

    /* warna hijau solid */
    background-color: #2e765e !important;

    /* bentuk pill */
    border-radius: 999px;
    border: none;

    /* ukuran nyaman */
    padding: 0.75rem 1.6rem;

    /* bayangan lembut */
    box-shadow: 0 12px 24px rgba(15, 23, 42, 0.35);

    /* teks */
    color: #ffffff;
    font-weight: 700;
}

.hero-box .btn-primary:hover {
    filter: brightness(1.05);
    transform: translateY(-1px);
    box-shadow: 0 16px 30px rgba(15, 23, 42, 0.4);
}

/* Layout Tentang Kami */
.about-layout {
    max-width: 100%;
    position: relative;
}

/* Gambar kiri, teks mengalir di kanan dan bawahnya */
.about-photo {
    float: left;
    width: min(280px, 35%);
    margin-right: 28px;
    margin-bottom: 18px;
    border-radius: 20px;
    display: block;
}

/* Judul berada di kolom kanan */
.about-title {
    margin-top: 0;
    margin-bottom: 1rem;
    font-size: 2rem;
    font-weight: 700;
}

/* Clear float setelah teks selesai */
.about-copy::after {
    content: "";
    display: block;
    clear: both;
}

/* Typography */
.about-copy p,
.about-copy li {
    font-size: 1rem;
    line-height: 1.65;
    color: #374151;
    margin-bottom: 0.7rem;
}

/* Bold h3 */
.about-copy h3 {
    margin-top: 1.3rem;
    margin-bottom: 0.3rem;
    font-weight: 700;
}

/* Responsif mobile */
@media (max-width: 768px) {
    .about-photo {
        float: none;
        width: 100%;
        margin-right: 0;
        margin-bottom: 20px;
    }

    .about-title {
        margin-top: 0.5rem;
    }
}

/* Label kecil di atas peta (opsional) */
.map-caption {
    margin-top: 1rem;
    margin-bottom: 0.25rem;
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #6b7280; /* abu-abu lembut */
    font-weight: 600;
}

/* MAP CARD – versi premium */
.location-map-embed {
    position: relative;
    width: 100%;
    padding-top: 56%; /* rasio kira-kira 16:10 */
    border-radius: 22px;
    overflow: hidden;

    margin-top: 1rem; /* jarak dari alamat ke map */
    margin-bottom: 1rem; /* jarak map ke tombol */

    /* fallback background saat map belum load */
    background: radial-gradient(
        circle at 10% 20%,
        #e8f4fc 0,
        #d5e8f5 35%,
        #c8deef 70%,
        #b0d2e7 100%
    );

    box-shadow: 0 18px 40px rgba(15, 23, 42, 0.18);
    transition:
        transform 0.25s ease,
        box-shadow 0.25s ease;
}

/* Inner border halus di pinggir map */
.location-map-embed::before {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: inherit;
    box-shadow:
        inset 0 0 0 1px rgba(255, 255, 255, 0.55),
        inset 0 1px 0 rgba(255, 255, 255, 0.65);
    pointer-events: none;
}

/* Iframe isi map */
.location-map-embed iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
    filter: saturate(1.05) contrast(1.03);
    transition:
        transform 0.25s ease,
        filter 0.25s ease;
}

/* Hover di seluruh card: map sedikit “angkat” dan zoom halus */
.glass-card:hover .location-map-embed {
    transform: translateY(-3px);
    box-shadow: 0 26px 55px rgba(15, 23, 42, 0.24);
}

.glass-card:hover .location-map-embed iframe {
    transform: scale(1.02);
    filter: saturate(1.1) contrast(1.05);
}

/* Responsif – di layar kecil biarkan sama, sudah fleksibel */
@media (max-width: 640px) {
    .location-map-embed {
        border-radius: 18px;
        padding-top: 70%; /* sedikit lebih tinggi di mobile */
    }
}

/* Map embed styling upgrade */
.location-map-embed {
    position: relative;
    width: 100%;
    padding-top: 56%;
    border-radius: 22px;
    overflow: hidden;
    background: #dfe9ef;
    box-shadow: 0 18px 40px rgba(15, 23, 42, 0.18);
    margin-top: 1rem;
    margin-bottom: 1rem;
}

.location-map-embed iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
}

/* Jam operasional */
.operation-hours p {
    font-size: 0.95rem;
    color: #1f2937;
}

.operation-hours ul {
    margin: 6px 0 0 0;
    padding-left: 20px;
    color: #374151;
    font-size: 0.9rem;
}

.operation-hours li {
    margin-bottom: 2px;
}

/* =========================================================
   Rekanan / Asuransi – Glass-Light (Logo Version)
========================================================= */

.insurance-item {
    display: flex;
    align-items: center;
    justify-content: center;

    width: 180px;
    height: 90px;

    padding: 20px;
    border-radius: 18px;

    /* Glass effect */
    background: rgba(255, 255, 255, 0.65);
    border: 1px solid rgba(46, 118, 94, 0.55);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);

    box-shadow: 0 10px 28px rgba(15, 23, 42, 0.14);
    transition: 0.2s ease;

    /* Prevent layout shift */
    overflow: hidden;
}

/* Logo image inside */
.insurance-item img {
    height: 90px; /* paksa semua logo tinggi 40px */
    width: auto; /* lebar mengikuti proporsi */
    max-width: 170px;
    object-fit: contain; /* jaga proporsi logo */
    pointer-events: none; /* logo tidak perlu interaksi */
    filter: saturate(1.1); /* sedikit lebih hidup */
    opacity: 0.92; /* supaya serasi dengan glass */
    transition: opacity 0.2s ease;
}

.insurance-item:hover img {
    opacity: 1;
}

.partners-grid {
    display: grid;
    gap: 24px;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    justify-items: center;
}
